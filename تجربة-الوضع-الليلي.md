# 🌙 إزاي تجرّب الوضع الليلي

عندك **٣ طرق** — مرتّبة من الأسهل للأصعب. اختار اللي يناسبك.

---

## ⭐ الطريقة ١: من المتصفح مباشرة (Codespaces) — من غير ما تنزّل حاجة

دي أسهل حاجة خالص، هتشتغل من أي جهاز حتى من الموبايل.

1. افتح المستودع على GitHub:
   👉 https://github.com/fantazia1110-design/BRAVO-new-sestym-

2. فوق على الشمال، اضغط زرار **Branch** واختار الفرع:
   **`arena/019fa154-bravo-new-sestym`**

   > ⚠️ مهم جداً تختار الفرع ده، لأن الوضع الليلي موجود فيه مش في `main`.

3. اضغط الزرار الأخضر **`< > Code`** ← تبويب **`Codespaces`** ← **`Create codespace on arena/...`**

4. استنى ١-٢ دقيقة (بيثبّت الحزم لوحده)، وبعدها هيفتح لك المشروع شغال في نافذة معاينة جوه المتصفح.

   لو المعاينة مفتحتش لوحدها: افتح تبويب **PORTS** تحت واضغط على 🌐 جنب بورت **3000**.

**مجاني** لحد ٦٠ ساعة شهرياً على الحسابات المجانية.

---

## 💻 الطريقة ٢: على جهازك

محتاج **Node.js نسخة 20 أو أحدث** ([نزّله من هنا](https://nodejs.org)).

افتح Terminal / CMD واكتب:

```bash
git clone https://github.com/fantazia1110-design/BRAVO-new-sestym-.git
cd BRAVO-new-sestym-
git checkout arena/019fa154-bravo-new-sestym
npm install
npm run dev
```

بعدها افتح المتصفح على 👈 **http://localhost:3000**

> ✅ **مش محتاج قاعدة بيانات** — كل الشاشات بتفتح عادي ببيانات تجريبية.

### لو المشروع منزّل عندك من قبل
```bash
cd BRAVO-new-sestym-
git fetch origin
git checkout arena/019fa154-bravo-new-sestym
git pull
npm install
npm run dev
```

---

## 🌐 الطريقة ٣: رابط دائم على GitHub Pages (اختياري)

لو عايز رابط ثابت تفتحه في أي وقت أو تبعته لحد:

1. في المستودع، اعمل مجلد `.github/workflows/`
2. انسخ فيه الملف الجاهز: **`docs/github-pages-workflow.yml`** وسمّيه `deploy-preview.yml`
3. من **Settings ← Pages ← Source** اختار **GitHub Actions**
4. بعد ما الـ Action يخلص، الرابط هيكون:
   `https://fantazia1110-design.github.io/BRAVO-new-sestym-/`

> ملاحظة: النسخة دي للمعاينة البصرية بس (واجهات API متشتغلش في الاستضافة الثابتة).

---

# 🎛️ التبديل بين الليلي والنهاري

### 1️⃣ الزرار السريع (الأسهل)
انزل لآخر **القائمة الجانبية** على اليمين — فوق زرار "تسجيل الخروج" مباشرة:

- 🌙 **الوضع الداكن** → اضغط عشان تدخل الوضع الليلي
- ☀️ **الوضع الفاتح** → اضغط عشان ترجع النهاري

### 2️⃣ من صفحة الإعدادات
**الإعدادات** ← تبويب **المظهر** ← قسم **الوضع**

| الاختيار | الوصف |
|---|---|
| ☀️ **فاتح** | الوضع النهاري الأصلي (زي ما هو بالظبط) |
| 🌙 **داكن** | الوضع الليلي الجديد (BRAVO Night) |
| 🖥️ **تلقائي** | يمشي على إعدادات الويندوز/الموبايل بتاعك |

---

## 🎨 اللي المفروض تشوفه في الوضع الليلي

- **الخلفية:** كحلي غامق `#070b22` بتوهجات بنفسجية وفوشيا
- **الكروت:** كحلي فاتح بحواف بنفسجية شفافة وظلال عميقة
- **القائمة الجانبية:** عناصر على شكل كبسولات، والعنصر المفتوح بتوهج بنفسجي
- **الأرقام والعناوين:** تدرج أبيض ← بنفسجي فاتح
- **الأزرار الأساسية:** بنفسجي نيون `#7c5cff` بتوهج

---

## ✅ حاجات مهمة

- اختيارك **بيتحفظ في المتصفح** — لو قفلت وفتحت تاني هيفضل زي ما سيبته
- **مفيش وميض أبيض** عند فتح الموقع وأنت على الوضع الداكن
- لو فاتح أكتر من تبويب، التبديل **بيتزامن بينهم** أوتوماتيك
- **الوضع النهاري متغيّرش فيه ولا حاجة** — نفس الشكل الأصلي ١٠٠٪

---

## 🛠️ حل المشاكل

**البورت 3000 مشغول:**
```bash
npm run dev -- -p 3001
```

**مشكلة في التثبيت:**
```bash
rm -rf node_modules .next
npm install
npm run dev
```

**مش لاقي الوضع الليلي:** اتأكد إنك على الفرع الصح:
```bash
git branch --show-current
# لازم يطلع: arena/019fa154-bravo-new-sestym
```

---

## 📋 أوامر مفيدة

```bash
npm run dev        # تشغيل نسخة التطوير
npm run build      # بناء نسخة الإنتاج
npm run lint       # فحص الكود
npm run typecheck  # فحص أنواع TypeScript
```
